version: '3'

services:
  docs:
    image: pulse_ai_local_docs
    container_name: pulse_ai_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./pulse_ai:/app/pulse_ai:z
    ports:
      - '9000:9000'
    command: /start-docs
